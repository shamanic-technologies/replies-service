# Neon Database (reply-qualification-service)
REPLY_QUALIFICATION_SERVICE_DATABASE_URL='postgresql://...'

# Service-to-service auth (this service)
REPLY_QUALIFICATION_SERVICE_API_KEY='...'

# Chat Service (LLM completion via Gemini/Anthropic; resolves provider keys internally)
CHAT_SERVICE_URL='http://chat-service.railway.internal:8080'
CHAT_SERVICE_API_KEY='...'

# Runs Service (run lifecycle: create + status; chat-service logs LLM costs on child runs)
RUNS_SERVICE_URL='http://runs-service.railway.internal:8080'
RUNS_SERVICE_API_KEY='...'
